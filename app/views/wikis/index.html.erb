</div> </h1>
  <div class="row">
    <div class="row header-container">
      <h1 class="wiki-center col-md-5 col-md-offset-2"> Public Wikis </h1>
      <%= link_to "New Wiki", new_wiki_path, class: 'btn btn-success col-md-1 align-items-center new-wiki-button' %>
    </div>
    <div class="box-solid col-md-8 col-md-offset-2">
      <% @wikis.each do |wiki| %>
        <div class="media wiki-shadow">
          <div class="media-body">
            <h4 class="media-heading">
              <%= link_to markdown(wiki.title), wiki %> <% if wiki.private?%> PRIVATE <% end %>
            </h4>
            <small>
              <%= markdown(wiki.body) %>
            </small>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
